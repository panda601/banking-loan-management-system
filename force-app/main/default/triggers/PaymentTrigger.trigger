/**
 * @description Legacy Payment trigger stub - inactive in favor of BLMS_PaymentTrigger
 */
trigger PaymentTrigger on Payment__c (before insert) {
    // Legacy trigger disabled for BLMS enterprise framework
}
